{
  "name": "git-graph-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch": "nodemon --watch src --ext js,ts,vue,jsx,tsx --exec \"npm run build\""
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1"
  }
}
